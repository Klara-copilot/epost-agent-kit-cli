---
phase: 1
title: "Pre-release hygiene"
effort: 30m
depends: []
---

# Phase 01 — Pre-release hygiene

Resolve dirty tree, create missing LICENSE, audit README version refs.

## Context

Working tree has uncommitted drift. Must be clean before tagging.
Also `LICENSE` is declared in `package.json` `files` allowlist but missing on disk.

## Requirements

- Clean working tree on `master`
- `LICENSE` file present at repo root (MIT, matches `package.json` `license` field)
- README has no stale version pins (v0.0.1/v0.0.2 references outside of historical context)

## Related Files

- `docs/proposals/signals.json` — modified (timestamp-only drift from signals regeneration)
- `plans/reports/260409-1110-get-started-env-verify-epost-fullstack-developer.md` — untracked
- `plans/reports/260409-1110-kb-verification-epost-docs-manager.md` — untracked
- `LICENSE` — missing at root, need to create (MIT, author "Phuong Doan", year 2026)
- `README.md` — audit for version references
- `package.json` lines 21-25 (files allowlist)

## Steps

1. Inspect `signals.json` diff — confirm timestamp-only regen (already verified: only `generated` field changed)
2. Create `LICENSE` at repo root with MIT template, author `Phuong Doan`, year `2026`
3. Grep `README.md` for literal `0.0.1` / `0.0.2` — if present outside an obvious history section, note for phase-02 update
4. Stage all three hygiene items together:
   - `git add LICENSE docs/proposals/signals.json plans/reports/260409-1110-*.md`
5. Commit as `chore: pre-release hygiene for v0.1.0 (LICENSE, signals regen, reports)`
6. Do NOT push yet — phase-04 pushes after phase-02 + phase-03 pass

## Todo

- [ ] Verify `signals.json` diff is timestamp-only (benign)
- [ ] Create `LICENSE` (MIT, Phuong Doan, 2026)
- [ ] Audit `README.md` for stale version pins
- [ ] Stage 3 hygiene items
- [ ] Commit with `chore:` prefix
- [ ] Confirm `git status` clean

## Success Criteria

- `git status --short` returns empty (no modified, no untracked)
- `ls LICENSE` succeeds at repo root
- `cat LICENSE | head -1` shows MIT header
- Commit sha visible in `git log -1`

## Risks

| Risk | Mitigation |
|---|---|
| README has hard-coded `0.0.2` pin | Fix inline in this phase or defer to phase-02 |
| `signals.json` regen is non-deterministic (timestamp) — will redirty on next tool run | Accept — any re-drift after phase-01 must be re-committed or stashed in phase-04 |
| LICENSE content drift from declared MIT | Use standard OSI MIT template verbatim |

## Unresolved

- Is `signals.json` being re-generated by an automatic hook on every session? If yes, adding to `.gitignore` might be cleaner than re-committing — but that's an orthogonal concern for a separate plan. For now: commit as-is.
